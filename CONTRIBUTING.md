# Contributing

Each package is standalone. Install, build, and test the package you change from its own directory. A change to the generated project must also pass the packed-SDK integration test.

Open a pull request with tests and a concise compatibility/semver note for each affected package. Package versions and tags remain independent.
