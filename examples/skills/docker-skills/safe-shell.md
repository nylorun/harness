# Safe shell workflow

Inspect the workspace first. Use short, deterministic shell commands. Do not request network access,
install packages, or access files outside the workspace. Explain command output before proposing changes.
