// Kept in step with package.json by runtime/scripts/check-package.mjs, which runs in npm run check.
// release:prepare also rewrites this when it bumps @nylorun/runtime.
export const RUNTIME_VERSION = "0.9.0-beta";
