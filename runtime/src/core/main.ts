/**
 * Former env-driven process entry. Runtime Host entry is `host/main.ts` (WS-C).
 * Ambient reads removed so WS-A ambient grep stays empty.
 */
console.error(
  "@nylorun/runtime server entry moved to host/main.ts (Runtime Tenants Wave 1)",
);
process.exit(1);
